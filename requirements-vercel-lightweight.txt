# Ultra-lightweight requirements for Vercel deployment
# VERCEL FIX METHOD A: Core functionality only

# Core FastAPI dependencies (minimal)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTTP client for external requests
requests==2.31.0

# Data processing (essential only)
python-dateutil==2.8.2

# Build dependencies for Python 3.12 compatibility
setuptools>=68.0.0
packaging>=23.0

# Standard library dependencies
typing-extensions>=4.8.0
pydantic>=2.0.0